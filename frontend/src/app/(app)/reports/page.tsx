import { ModulePage } from "@/components/module-page";

export default function ReportsPage() {
  return (
    <ModulePage
      eyebrow="Reports"
      title="Executive report"
      description="Report generation is not connected yet. It will summarize validated findings, not raw model output."
    />
  );
}
