import { ModulePage } from "@/components/module-page";

export default function DataQualityPage() {
  return (
    <ModulePage
      eyebrow="Data"
      title="Data quality"
      description="Quality checks will run after eligible datasets are mapped. Nothing is connected yet."
    />
  );
}
