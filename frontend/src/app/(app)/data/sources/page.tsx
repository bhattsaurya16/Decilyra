import { ModulePage } from "@/components/module-page";

export default function DataSourcesPage() {
  return (
    <ModulePage
      eyebrow="Data"
      title="Sources"
      description="Connect a data source to begin. CSV ingestion and live connectors are planned for later phases."
    />
  );
}
