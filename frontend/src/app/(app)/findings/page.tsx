import { ModulePage } from "@/components/module-page";

export default function FindingsPage() {
  return (
    <ModulePage
      eyebrow="Decisions"
      title="Findings"
      description="Findings will appear after deterministic analysis produces validated results. No methods have run yet."
    />
  );
}
