import { ModulePage } from "@/components/module-page";

export default function ScenariosPage() {
  return (
    <ModulePage
      eyebrow="Decisions"
      title="Scenarios"
      description="Scenario modeling will open after a baseline analysis exists. Connect data first."
    />
  );
}
